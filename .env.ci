PYTHONPATH=.
APP_ENV=ci
DB_URL="postgresql+asyncpg://test_user:test_password@localhost:5433/test_newsdb"
DB_POOL_SIZE=12

# 在 make test ARGS='--record-mode=rewrite -s' 时需要注释该掉，避免覆盖 .env 里的真实 LLM_API_KEY
LLM_API_KEY="DUMMY"
